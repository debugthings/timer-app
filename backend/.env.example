DATABASE_URL="file:./dev.db"
PORT=3001
NODE_ENV=development

# Optional: Home Assistant webhook called when a checkout completes (POST with form data)
# CHECKOUT_COMPLETE_WEBHOOK_URL=https://homeassistant/api/webhook/timer_is_up_webhook
